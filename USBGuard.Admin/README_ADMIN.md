USBGuard.Admin (WinForms)

- .NET Framework 4.8 WinForms project.
- Creates `device.auth` files using SHA-256 integrity placed in Reserved area.
- Ensure the registry key `HKEY_LOCAL_MACHINE\SOFTWARE\USBGuard\AppID` is set for the agent to validate.
